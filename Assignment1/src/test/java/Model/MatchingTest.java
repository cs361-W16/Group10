package Model;

import org.junit.Assert;
import org.junit.Test;

/**
 * Created by Charles on 1/22/2016.
 */
public class MatchingTest {

    public void TestMatching (){
         /* In order to test the matching function we would need to be able to
            first create a certain order of cards in the four columns to serve as the
            standard condition. unfortunately, we have no idea how to do this.
            if we did, this would be a very impressive test, I assure you.
          */

    }

    public void TestFindMatches () {
            /* this test could be run by way of the scoring incrementer.
                we would run the Findmatches function for a situation expecting x
                matches, and see whether the score was increased properly.
             */
    }
}
